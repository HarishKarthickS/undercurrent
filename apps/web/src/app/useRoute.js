import { useEffect, useState } from 'react';
export function useRoute() { const [location,setLocation]=useState(()=>({ path:window.location.pathname, search:window.location.search })); useEffect(()=>{const onPop=()=>setLocation({ path:window.location.pathname, search:window.location.search }); addEventListener('popstate',onPop); return()=>removeEventListener('popstate',onPop);},[]); const go=(to)=>{if(to!==`${window.location.pathname}${window.location.search}`){history.pushState({},'',to);setLocation({ path:window.location.pathname, search:window.location.search });} window.scrollTo(0,0);}; return { ...location, go }; }
