export const parentSessionSchema = { type: 'object', additionalProperties: false, required: ['token', 'expiresAt', 'parent', 'household'], properties: { token: { type: 'string' }, expiresAt: { type: 'string' }, parent: { type: 'object', additionalProperties: false, required: ['id', 'displayName'], properties: { id: { type: 'string' }, displayName: { type: 'string' } } }, household: { type: 'object', additionalProperties: false, required: ['id', 'name'], properties: { id: { type: 'string' }, name: { type: 'string' } } } } };
const email = { type: 'string', format: 'email', maxLength: 254 };
export const signupSchema = { body: { type: 'object', additionalProperties: false, required: ['displayName', 'email', 'password', 'invitationToken'], properties: { displayName: { type: 'string', minLength: 1, maxLength: 100 }, email, password: { type: 'string', minLength: 12, maxLength: 256 }, invitationToken: { type: 'string', minLength: 20, maxLength: 256 } } } };
export const loginSchema = { body: { type: 'object', additionalProperties: false, required: ['email', 'password'], properties: { email, password: { type: 'string', minLength: 1, maxLength: 256 } } } };
export const tokenSchema = { body: { type: 'object', additionalProperties: false, required: ['token'], properties: { token: { type: 'string', minLength: 1, maxLength: 256 } } } };
export const verifySchema = { body: { type: 'object', additionalProperties: false, required: ['parentId', 'token'], properties: { parentId: { type: 'string', format: 'uuid' }, token: { type: 'string', minLength: 1, maxLength: 256 } } } };
export const resetRequestSchema = { body: { type: 'object', additionalProperties: false, required: ['email'], properties: { email } } };
export const resetCompleteSchema = { body: { type: 'object', additionalProperties: false, required: ['token', 'password'], properties: { token: { type: 'string', minLength: 1, maxLength: 256 }, password: { type: 'string', minLength: 12, maxLength: 256 } } } };
export const termsSchema = { body: { type: 'object', additionalProperties: false, required: ['accepted'], properties: { accepted: { const: true } } } };
export const guardianInviteSchema = { body: { type: 'object', additionalProperties: false, required: ['email'], properties: { email, role: { type: 'string', enum: ['owner', 'guardian'] } } } };
