CREATE TABLE "household_preferences" ("household_id" uuid PRIMARY KEY NOT NULL REFERENCES "households"("id"), "locale" text DEFAULT 'en' NOT NULL, "daily_digest_enabled" boolean DEFAULT true NOT NULL, "weekly_digest_enabled" boolean DEFAULT true NOT NULL, "updated_at" timestamp with time zone DEFAULT now() NOT NULL);
--> statement-breakpoint
CREATE TABLE "parent_child_preferences" ("id" uuid PRIMARY KEY DEFAULT gen_random_uuid() NOT NULL, "parent_id" uuid NOT NULL REFERENCES "parent_accounts"("id"), "student_id" uuid NOT NULL REFERENCES "students"("id"), "dashboard_layout" jsonb DEFAULT '[]'::jsonb NOT NULL, "guidance_mode" text DEFAULT 'gentle' NOT NULL, "goal_type" text DEFAULT 'sessions' NOT NULL, "goal_target" integer DEFAULT 3 NOT NULL, "transcript_consent" boolean DEFAULT false NOT NULL, "advisor_consent" boolean DEFAULT false NOT NULL, "updated_at" timestamp with time zone DEFAULT now() NOT NULL, CONSTRAINT "parent_child_preferences_parent_student_unique" UNIQUE("parent_id","student_id"));
